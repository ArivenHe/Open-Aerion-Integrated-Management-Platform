spring:
  application:
    name: openaimp-backend
  datasource:
    url: jdbc:mysql://localhost:3306/openaimp?serverTimeZone=UTC
    username: root
    password: ${DB_PASSWORD:ab321818}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5s
  mail:
    host: smtp.qq.com
    port: 587
    username: amstudio@qq.com
    password: ojxeivkzyineddaj
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: false
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

sa-token:
  token-name: aerionstudio
  timeout: 2592000
  active-timeout: -1
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: true

server:
  port: 6067

app:
  mail:
    from: amstudio@qq.com
  bootstrap:
    default-user-cid: ${BOOTSTRAP_DEFAULT_USER_CID:1}
    default-user-email: ${BOOTSTRAP_DEFAULT_USER_EMAIL:admin@openaimp.local}
    default-user-password: ${BOOTSTRAP_DEFAULT_USER_PASSWORD:}
    platform-name: ${BOOTSTRAP_PLATFORM_NAME:OpenAIMP}
    platform-description: ${BOOTSTRAP_PLATFORM_DESCRIPTION:Open Aerion Integrated Management Platform}
    platform-logo: ${BOOTSTRAP_PLATFORM_LOGO:}
    platform-url: ${BOOTSTRAP_PLATFORM_URL:}
    platform-signed-user-count: ${BOOTSTRAP_PLATFORM_SIGNED_USER_COUNT:1}
    platform-create-time: ${BOOTSTRAP_PLATFORM_CREATE_TIME:}
  fsd:
    service-base-url: ${FSD_SERVICE_BASE_URL:http://localhost:8000}
    web-base-url: ${FSD_WEB_BASE_URL:http://localhost:8000}
    jwt-secret: ${FSD_JWT_SECRET:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJvcGVuZnNkIiwiZXhwIjoxOTIzNjk2MDAwLCJuYmYiOjE3NzY0MTMxNjksImlhdCI6MTc3NjQxMzE5OSwianRpIjoiMmI3OGEzOTktNmYzNy00YTBhLThmNDMtYzg0MTFjMzI4YTY3IiwidG9rZW5fdHlwZSI6ImFjY2VzcyIsImNpZCI6MSwibmV0d29ya19yYXRpbmciOjEyfQ.bZp9Dc74mPy23m4kUGrbGSd-uuYVyCJLWgQ44fmdfmU}
    # 优先使用现成的 openfsd API token；未配置时，会用下面的超管 CID/密码登录 openfsd 获取 access_token。
    api-access-token: ${FSD_API_ACCESS_TOKEN:}
    service-cid: ${FSD_SERVICE_CID:1}
    service-password: ${FSD_SERVICE_PASSWORD:410b5bfb1696bc3f}
    service-first-name: ${FSD_SERVICE_FIRST_NAME:OpenAIMP}
    service-last-name: ${FSD_SERVICE_LAST_NAME:Backend}
    service-network-rating: ${FSD_SERVICE_NETWORK_RATING:12}
    service-token-validity-seconds: ${FSD_SERVICE_TOKEN_VALIDITY_SECONDS:900}
    request-timeout-seconds: ${FSD_SERVICE_REQUEST_TIMEOUT_SECONDS:10}
    default-user-network-rating: ${FSD_DEFAULT_USER_NETWORK_RATING:1}
