spring:
  application:
    name: openaimp-backend
  datasource:
    url: jdbc:mysql://localhost:3306/openaimp?serverTimeZone=UTC
    username: root
    password: ${DB_PASSWORD:ab321818}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5s
  mail:
    host: smtp.qq.com
    port: 587
    username: amstudio@qq.com
    password: ojxeivkzyineddaj
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: false
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

sa-token:
  token-name: aerionstudio
  timeout: 2592000
  active-timeout: -1
  is-concurrent: true
  is-share: false
  token-style: uuid
  is-log: true

server:
  port: 6067

app:
  mail:
    from: amstudio@qq.com
  fsd:
    service-base-url: ${FSD_SERVICE_BASE_URL:http://localhost:13618}
    web-base-url: ${FSD_WEB_BASE_URL:http://localhost:8080}
    jwt-secret: ${FSD_JWT_SECRET:}
    service-cid: ${FSD_SERVICE_CID:1}
    service-first-name: ${FSD_SERVICE_FIRST_NAME:OpenAIMP}
    service-last-name: ${FSD_SERVICE_LAST_NAME:Backend}
    service-network-rating: ${FSD_SERVICE_NETWORK_RATING:12}
    service-token-validity-seconds: ${FSD_SERVICE_TOKEN_VALIDITY_SECONDS:900}
    request-timeout-seconds: ${FSD_SERVICE_REQUEST_TIMEOUT_SECONDS:10}
    default-user-network-rating: ${FSD_DEFAULT_USER_NETWORK_RATING:1}
