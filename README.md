# Open-Aerion-Integrated-Management-Platform (Open-AIMP)

Open-AIMP 是一个专为飞行模拟环境设计的综合飞控中心。它提供了模拟飞行操作、用户管理和系统监控的集成管理能力。

## 🚀 功能特性

*   **飞控中心**：管理模拟飞行操作的核心功能模块。
*   **微服务架构**：基于微服务设计，具有良好的可扩展性和可维护性。
*   **用户管理**：集成 Sa-Token，提供安全的认证和授权机制。
*   **网关服务**：统一的 API 网关，负责路由转发和安全控制。

## 🛠 技术栈

### 后端 (Backend)
*   **开发语言**：Java 17
*   **核心框架**：Spring Boot 3.2.4
*   **微服务框架**：Spring Cloud 2023.0.1, Spring Cloud Alibaba 2023.0.1.0
*   **服务注册与配置**：Nacos
*   **网关**：Spring Cloud Gateway
*   **安全认证**：Sa-Token (JWT, Redis)
*   **数据库**：MySQL 8.0, Redis
*   **ORM 框架**：Spring Data JPA

### 前端 (Frontend)
*   **框架**：Vue.js 3
*   **构建工具**：Vite
*   **语言**：JavaScript/HTML/CSS

## 📂 项目结构

```
open-aimp/
├── backend/                # 后端微服务代码
│   ├── aimp-gateway/       # API 网关服务 (端口: 9000)
│   ├── aimp-user/          # 用户服务 (端口: 8080)
│   └── pom.xml             # 父级 Maven 配置
├── frontend/               # Vue.js 前端应用
└── README.md               # 项目说明文档
```

## 🏁 快速开始

### 环境要求

请确保您的开发环境已安装以下软件：
*   **Java JDK**: 版本 17 或更高
*   **Maven**: 版本 3.6 或更高
*   **Node.js**: 版本 20.19.0+
*   **MySQL**: 版本 8.0+
*   **Redis**: 最新稳定版
*   **Nacos**: Server 版本 2.x

### 部署步骤

#### 1. 基础设施准备
1.  启动 **MySQL** 服务器，并创建一个名为 `aimp` 的数据库。
2.  启动 **Redis** 服务器。
3.  启动 **Nacos** 服务器 (默认地址: `localhost:8848`)。

#### 2. 后端启动
1.  进入后端目录：
    ```bash
    cd backend
    ```
2.  检查配置文件，确保数据库和 Nacos 配置与您的环境一致：
    *   `aimp-user/src/main/resources/application.yml` (MySQL, Redis 配置)
    *   `aimp-user/src/main/resources/bootstrap.yml` (Nacos 配置)
    *   `aimp-gateway/src/main/resources/application.yml` (Nacos, Redis 配置)
3.  构建项目：
    ```bash
    mvn clean install
    ```
4.  启动服务：
    *   **Gateway**: 运行 `aimp-gateway` 模块下的 `GatewayApplication.java`。
    *   **User Service**: 运行 `aimp-user` 模块下的 `AimpApplication.java`。

#### 3. 前端启动
1.  进入前端目录：
    ```bash
    cd frontend
    ```
2.  安装依赖：
    ```bash
    npm install
    ```
3.  启动开发服务器：
    ```bash
    npm run dev
    ```
4.  根据 Vite 提示的 URL 访问应用 (通常是 `http://localhost:5173`)。

## 🤝 参与贡献

欢迎提交 Pull Request 或 Issue 来参与项目贡献！
